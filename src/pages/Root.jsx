import {Navbar, Footer} from '../components/root';

function Root() {
    return (
        <main className='Root'>
            <Navbar />
            <Footer />
        </main>
    );
}

export default Root;